import "package:flutter/material.dart";

const double registrationBoxPadding = 30.0;

class RegistrationBox extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(
        registrationBoxPadding,
      ),
      child: Column(
        children: [],
      ),
    );
  }
}
